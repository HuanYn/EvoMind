{
  "run_dir": "artifacts/runs/text_official_mini_20260908",
  "description": "Pinned official mini full datasets, 2+2 epochs; serialization fixes and Windows worker/memory adaptations recorded.",
  "stages": [
    {
      "name": "pretrain",
      "argv": ["train_pretrain.py", "--epochs", "2", "--batch_size", "32", "--accumulation_steps", "8", "--learning_rate", "5e-4", "--max_seq_len", "340", "--hidden_size", "768", "--num_hidden_layers", "8", "--dtype", "bfloat16", "--num_workers", "0", "--log_interval", "100", "--save_interval", "1000", "--from_weight", "none", "--from_resume", "0", "--seed", "42"],
      "requires": ["artifacts/provenance/text_assets.json", "dataset/pretrain_t2t_mini.jsonl"],
      "expected_weight": "pretrain"
    },
    {
      "name": "full_sft",
      "argv": ["train_full_sft.py", "--epochs", "2", "--batch_size", "8", "--accumulation_steps", "2", "--learning_rate", "1e-5", "--max_seq_len", "768", "--hidden_size", "768", "--num_hidden_layers", "8", "--dtype", "bfloat16", "--num_workers", "0", "--log_interval", "200", "--save_interval", "2000", "--from_weight", "pretrain", "--from_resume", "0", "--seed", "42"],
      "requires": ["dataset/sft_t2t_mini.jsonl"],
      "expected_weight": "full_sft"
    },
    {
      "name": "text_diagnostic",
      "cwd": ".",
      "argv": ["scripts/evomind_text_eval.py", "--checkpoint", "out/full_sft_768.pth", "--output-dir", "artifacts/evaluation/text_official_mini"],
      "requires": ["out/full_sft_768.pth"]
    },
    {
      "name": "vision_and_report",
      "cwd": ".",
      "argv": ["scripts/evomind_continue.py"],
      "requires": ["out/full_sft_768.pth", "scripts/evomind_continue.py"]
    }
  ]
}
