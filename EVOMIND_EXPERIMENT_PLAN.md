# evomind：执行中的复现与改进计划

> 2026-09-08 用户更正：文本不仅是预训练和SFT，后训练也必须对齐MiniMind-3。以下视觉方案保留为后续方案，暂不执行；完整文本分支及比较验收优先。视觉入口已设置硬门禁 `vision_enabled=false`。不要把下文原“SFT后转V”顺序当成最新执行指令。

本轮获授权后已执行，不再是路线建议。所有新代码、模型、数据、日志写入 `E:/project/Learning/evomind`。保留 MiniMind / MiniMind-V 的来源与许可证，不把改名当原创。旧 HappyLLM / evoLLM 的实验保留，不混入新结果。

## 一、基座：先得到可复现的参照

| 项目 | 本轮配置 |
|---|---|
| 官方文本版本 | `6fc918beb68a0d8c40452338df6319fe168014ba` |
| 官方视觉版本 | `740d467ece78a0b7d2d976fcb424472095d4a688` |
| 语言模型 | Dense，768维，8层，官方6400词表，63,912,192参数 |
| 预训练 | 完整 `pretrain_t2t_mini.jsonl`，1,270,238条，2epoch |
| 文本SFT | 完整 `sft_t2t_mini.jsonl`，905,718条，2epoch |
| 硬件适配 | 本机RTX 5060 Laptop 8GiB；workers=0；SFT B8×累积2 |
| 视觉编码器 | 固定 SigLIP2 base p32 256 的官方视觉编码器权重 |
| 视觉参考 | 官方SFT入口，文本SFT初始化，训练projector+LLM首尾层，2epoch |

文本预训练保持B32×累积8、LR5e-4、seq340；文本SFT LR1e-5、seq768。未把旧项目16K tokenizer混用进这次官方参照。官方原生脚本只做运行环境与保存边界安全修正，不能声称与作者发布权重逐位一致。

## 二、要验证的改进，而不是预设的结论

| 组 | 图像处理 | 视觉token | 编码器执行 | 要回答的问题 |
|---|---|---:|---|---|
| A | 单张全图 | 64 | 在线FP32、冻结 | 受控单图基线 |
| B | 全图+四个不重叠象限 | 320 | 在线FP32、冻结 | 多局部视图是否改善可见细节的利用？ |
| C | 同B | 320 | 读取FP32冻结特征 | 保持输出与梯度一致的条件下，能否降低重复训练成本？ |

共同设置：同一个文本SFT权重；projector按同seed重新初始化并比较SHA256；三组相同数据ID；20,000条可用训练对话；每组2epoch；B1×累积4；seq768；LR5e-6；训练seed为42、123、2026。A/B/C均只更新projector和LLM首尾层。

共同长度筛选按**五视图**预算进行。过长完整对话直接拒绝，不伪造EOS，不让A额外吃到B/C无法容纳的样本。训练采用assistant目标token加权的CE/梯度累积，与官方逐microbatch均值实现存在差异，因此官方完整数据参考与20k受控A不是单变量对照；主因果比较是A对B、B对C。

这里的20k是将全量manifest按固定seed的样本哈希重排后，筛出的pilot，避免直接截取源文件前部的语言/任务顺序偏置；不代表全数据最优性能。重排不更改按原图决定的split，官方train-only parquet保持原始顺序。三个seed衡量训练随机性，不是三个不同验证集。多视图增加token和计算，不能宣称与A等算力。

## 三、数据隔离和评测

官方 `sft_i2t.parquet` 完整扫描，原始图片字节SHA256决定分组；约96%train、2%val、2%test，实际条数以数据报告为准。同一张原图的所有对话和五个裁剪视图必须属于同一split。此方法隔离字节相同图片，不保证识别重新编码/相似图片，保留这一限制。

官方视觉训练也使用只含train图片组的原始schema parquet。这样最终test图片不会因为“先训官方基线再改进”提前泄漏。

- 验证：固定64条，只用于监控NLL/保留best-val，不用于伪造质量评分。
- 最终自动评测：固定test中符合共同长度预算的500条，贪心解码，最多128新token；记录全部输出与EOS/重复/耗时/显存。
- 开放描述不计算参考文本完全匹配准确率。明确有闭合标准答案的任务才算EM，OCR才算CER；未提供相应任务标注则留空。
- 人工盲评：混合各模型输出、匿名ID、单独保存解盲键。未收到真实评分前状态是“待人工”，绝不由脚本代填。
- 中文文本20条功能诊断不冒充C-Eval/CMMLU；标准中文知识评测不属于本次图像细节改进的充分证据。

## 四、数值与效率检查

C仅缓存视觉编码器输出，不能缓存需要训练的projector输出。正式运行前，用真实图片和编码器比较B/C特征、logits、loss和projector梯度，记录误差、容差、软件版本与TF32设置。CPU单元测试通过不等于真实CUDA数值检查通过。

缓存统计必须包含一次性构建时间、文件占用、后续训练耗时；分别报告一次使用和三seed复用的总成本，不能只报cache-hit训练速度。训练快慢也不等同在线服务推理快慢。

## 五、运行与恢复

当前主入口：`configs/text_official_mini.json` → 文本预训练 → 文本SFT → 中文诊断 → `scripts/evomind_continue.py`。

后续29个串行阶段的精确参数在 `configs/vision_pipeline.json`。视觉阶段先显存probe、真实cache parity、各组2步smoke，再完整参考/多seed训练与评测。smoke保留完整2epoch学习率计划，正式运行从其checkpoint继续，不把2步训练冒充最终结果。

每个阶段成功后自动衔接；错误时保存日志并停止，避免静默更换实验条件。程序不会因终端关闭而随之退出，但系统关机/重启会中断，需要明确断点恢复；这不是操作系统开机自启服务。

checkpoint保存只在optimizer更新之后；自定义A/B/C保存optimizer、scaler、采样位置和RNG。自动保留最后两份step快照及best-val、last，减少磁盘增长；旧step快照会被清理，最后两份和best-val仍可恢复。

长期训练未完成时，报告保持“训练中/待测”。全部机器步骤跑完也不代表人评已完成，更不代表改进成立。只有结果支持时，才能在简历中写“提升了多少”。
